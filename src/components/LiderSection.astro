<section class="relative min-h-screen flex items-center bg-gradient-to-br from-slate-50 via-white to-red-50 text-black overflow-hidden">

    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 right-10 w-96 h-96 bg-red-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
        <div class="absolute bottom-20 left-10 w-96 h-96 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
        <div class="absolute top-1/2 left-1/2 w-96 h-96 bg-purple-100 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-4000"></div>
    </div>

    <div class="relative max-w-7xl mx-auto px-6 lg:px-8 py-16">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">

            <div class="opacity-0 -translate-x-10 transition-all duration-1000" data-leader>
                <div class="relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-red-600 via-purple-600 to-blue-600 rounded-3xl blur-lg opacity-25 group-hover:opacity-40 transition duration-500"></div>

                    <div class="relative bg-white p-2 rounded-3xl shadow-2xl transform group-hover:scale-[1.02] transition duration-500">
                        <img
                                src="/logo-martin-salas.png"
                                alt="Candidato al Senado del Perú"
                                class="w-full h-[560px] object-cover rounded-2xl"
                        />

                        <div class="absolute top-6 right-6 bg-red-600 text-white px-6 py-3 rounded-full shadow-xl font-bold text-sm backdrop-blur-sm bg-opacity-95 transform hover:scale-105 transition">
                            Candidato 2026
                        </div>
                    </div>

                    <div class="absolute -bottom-6 -right-6 w-32 h-32 bg-red-600 rounded-full opacity-10 blur-2xl"></div>
                </div>
            </div>

            <div class="opacity-0 translate-x-10 transition-all duration-1000 delay-200" data-leader>
                <div class="inline-flex items-center gap-2 mb-6 px-4 py-2 bg-red-50 border border-red-200 rounded-full">
                    <span class="w-2 h-2 bg-red-600 rounded-full animate-pulse"></span>
                    <span class="text-red-700 font-semibold text-sm uppercase tracking-wider">Liderazgo transformador</span>
                </div>

                <h2 class="text-4xl md:text-6xl font-extrabold mb-8 leading-tight">
                    Un liderazgo con
                    <span class="bg-gradient-to-r from-red-600 to-purple-600 bg-clip-text text-transparent">
                        experiencia y carácter
                    </span>
                </h2>

                <div class="space-y-6 mb-10">
                    <p class="text-lg md:text-xl text-gray-700 leading-relaxed">
                        Con una trayectoria sólida en el servicio público y privado, el candidato
                        representa una
                        <span class="font-semibold text-gray-900 border-b-2 border-red-300">nueva forma de hacer política</span>:
                        firme, transparente y cercana a la gente.
                    </p>

                    <p class="text-lg md:text-xl text-gray-700 leading-relaxed">
                        Su compromiso es claro: defender los intereses del Perú y legislar pensando en el futuro
                        de todas las regiones.
                    </p>
                </div>

                <div class="grid grid-cols-3 gap-6 mb-10 p-6 bg-white/60 backdrop-blur-sm rounded-2xl border border-gray-200 shadow-lg">
                    <div class="text-center">
                        <div class="text-3xl font-bold text-red-600 mb-1">20+</div>
                        <div class="text-sm text-gray-600 font-medium">Años de experiencia</div>
                    </div>
                    <div class="text-center border-x border-gray-200">
                        <div class="text-3xl font-bold text-red-600 mb-1">100%</div>
                        <div class="text-sm text-gray-600 font-medium">Compromiso</div>
                    </div>
                    <div class="text-center">
                        <div class="text-3xl font-bold text-red-600 mb-1">1</div>
                        <div class="text-sm text-gray-600 font-medium">Objetivo: Perú</div>
                    </div>
                </div>

                <div class="flex flex-wrap gap-4">
                    <a
                            href="#vision"
                            class="group relative inline-flex items-center gap-3 px-8 py-4 rounded-xl bg-gradient-to-r from-red-600 to-red-700 text-white font-semibold overflow-hidden shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300"
                    >
                        <span class="relative z-10">Conocer su visión de país</span>
                        <svg class="relative z-10 w-5 h-5 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                        </svg>
                        <div class="absolute inset-0 bg-gradient-to-r from-red-700 to-red-800 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left"></div>
                    </a>

                    <a
                            href="#propuestas"
                            class="inline-flex items-center gap-2 px-8 py-4 rounded-xl bg-white text-red-600 font-semibold border-2 border-red-600 hover:bg-red-50 transition-all duration-300 shadow-lg hover:shadow-xl"
                    >
                        Ver propuestas
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </a>
                </div>
            </div>

        </div>
    </div>

    <div class="absolute bottom-0 left-0 right-0 h-1 bg-gradient-to-r from-transparent via-red-600 to-transparent"></div>
</section>

<style>
    @keyframes blob {
        0%, 100% {
            transform: translate(0, 0) scale(1);
        }
        33% {
            transform: translate(30px, -50px) scale(1.1);
        }
        66% {
            transform: translate(-20px, 20px) scale(0.9);
        }
    }

    .animate-blob {
        animation: blob 7s infinite;
    }

    .animation-delay-2000 {
        animation-delay: 2s;
    }

    .animation-delay-4000 {
        animation-delay: 4s;
    }
</style>

<script>
    const leaderObserver = new IntersectionObserver(
        entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.remove(
                        'opacity-0',
                        'translate-x-10',
                        '-translate-x-10'
                    );
                }
            });
        },
        { threshold: 0.2 }
    );

    document.querySelectorAll('[data-leader]').forEach(el => leaderObserver.observe(el));
</script>