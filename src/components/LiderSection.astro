
<section id="liderazgo" class="relative overflow-hidden bg-gradient-to-br from-slate-50 via-white to-gray-50 py-32 md:py-40 background-pattern">

    <div class="absolute top-0 right-0 w-96 h-96 bg-red-600 opacity-5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-gray-900 opacity-5 rounded-full blur-3xl"></div>

    <div class="max-w-7xl mx-auto px-6 relative z-10">

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 mb-32 items-center">

            <div class="lg:col-span-6 opacity-0 animate-slide-in-left">
                <div class="inline-flex items-center gap-3 mb-8">
                    <div class="w-12 h-0.5 bg-red-600"></div>
                    <span class="text-xs font-black tracking-[0.5em] uppercase text-red-600">
                        Liderazgo
                    </span>
                </div>

                <h2 class="text-6xl md:text-7xl lg:text-8xl font-black leading-[0.95] mb-8">
                    <span class="block text-gray-900">
                        Capacidad
                    </span>
                    <span class="block text-gray-900">
                        para
                    </span>
                    <span class="block gradient-text mt-2">
                        gobernar
                    </span>
                    <span class="block text-red-600 mt-2">
                        con firmeza
                    </span>
                </h2>

                <div class="w-24 h-1 bg-red-600"></div>
            </div>

            <div class="lg:col-span-6 opacity-0 animate-slide-in-right delay-200">
                <div class="relative group">
                    <div class="absolute inset-0 bg-gradient-to-tr from-red-600 to-gray-900 opacity-10 group-hover:opacity-20 transition-opacity duration-500 rounded-2xl"></div>
                    <img
                            src="/logo-martin-salas.png"
                            alt="Candidato al Senado"
                            class="w-full h-[400px] object-contain relative z-10 floating"
                    />
                </div>
            </div>

        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-20 mb-32">

            <div class="lg:col-span-7 space-y-10 opacity-0 animate-fade-in-up delay-300">

                <p class="text-2xl md:text-3xl font-light text-gray-800 leading-relaxed">
                    El liderazgo que el país necesita no se construye con promesas,
                    sino con <span class="font-bold text-gray-900">experiencia</span>,
                    <span class="font-bold text-gray-900">criterio</span> y una
                    comprensión profunda de la realidad nacional.
                </p>

                <div class="h-px bg-gradient-to-r from-red-600 via-gray-300 to-transparent"></div>

                <p class="text-xl text-gray-700 leading-relaxed">
                    Este proyecto político se fundamenta en una forma de hacer política
                    <span class="font-bold text-red-600">
                        responsable, técnica y orientada a resultados
                    </span>,
                    con el objetivo de recuperar la confianza ciudadana y fortalecer
                    las instituciones del Estado.
                </p>

                <p class="text-xl text-gray-700 leading-relaxed">
                    Gobernar implica asumir decisiones complejas con visión de futuro,
                    defendiendo siempre los intereses del Perú y de sus regiones.
                </p>

            </div>

            <div class="lg:col-span-5 space-y-8 opacity-0 animate-fade-in-up delay-400">

                <div class="stat-card bg-white p-8 rounded-xl border-l-4 border-red-600 shadow-lg hover:shadow-2xl">
                    <p class="text-5xl md:text-6xl font-black text-gray-900 mb-3">+20</p>
                    <p class="text-sm font-bold uppercase tracking-widest text-red-600 mb-2">
                        Años de experiencia
                    </p>
                    <p class="text-gray-600 text-sm leading-relaxed">
                        Trayectoria comprobada en el sector público y privado
                    </p>
                </div>

                <div class="stat-card bg-white p-8 rounded-xl border-l-4 border-red-600 shadow-lg hover:shadow-2xl">
                    <p class="text-5xl md:text-6xl font-black text-red-600 mb-3">100%</p>
                    <p class="text-sm font-bold uppercase tracking-widest text-red-600 mb-2">
                        Compromiso con el Perú
                    </p>
                    <p class="text-gray-600 text-sm leading-relaxed">
                        Dedicación total al servicio del país y sus ciudadanos
                    </p>
                </div>

                <div class="stat-card bg-gradient-to-br from-gray-900 to-gray-800 p-8 rounded-xl shadow-lg hover:shadow-2xl">
                    <p class="text-4xl md:text-5xl font-black text-white mb-3">Visión País</p>
                    <p class="text-sm font-bold uppercase tracking-widest text-red-400 mb-2">
                        Futuro sostenible
                    </p>
                    <p class="text-gray-300 text-sm leading-relaxed">
                        Instituciones sólidas para las próximas generaciones
                    </p>
                </div>

            </div>

        </div>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-6 opacity-0 animate-scale-in delay-500">

            <a
                    href="#vision"
                    class="cta-button group relative inline-flex items-center justify-center px-14 py-7
                       bg-red-600 text-white text-lg font-black uppercase tracking-wider
                       hover:bg-red-700 hover:scale-105
                       transform transition-all duration-300 shadow-xl hover:shadow-2xl
                       rounded-lg overflow-hidden"
            >
                <span class="relative z-10 flex items-center gap-3">
                    Conocer visión de país
                    <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                    </svg>
                </span>
            </a>

            <a
                    href="#propuestas"
                    class="cta-button group relative inline-flex items-center justify-center px-14 py-7
                       bg-transparent border-3 border-gray-900 text-gray-900 text-lg font-black uppercase tracking-wider
                       hover:bg-gray-900 hover:text-white hover:scale-105
                       transform transition-all duration-300 shadow-xl hover:shadow-2xl
                       rounded-lg overflow-hidden"
            >
                <span class="relative z-10 flex items-center gap-3">
                    Ver propuestas
                    <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7"/>
                    </svg>
                </span>
            </a>

        </div>

    </div>
</section>

<style>
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes slideInLeft {
        from {
            opacity: 0;
            transform: translateX(-50px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @keyframes slideInRight {
        from {
            opacity: 0;
            transform: translateX(50px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @keyframes scaleIn {
        from {
            opacity: 0;
            transform: scale(0.9);
        }
        to {
            opacity: 1;
            transform: scale(1);
        }
    }

    .animate-fade-in-up {
        animation: fadeInUp 0.8s ease-out forwards;
    }

    .animate-slide-in-left {
        animation: slideInLeft 0.8s ease-out forwards;
    }

    .animate-slide-in-right {
        animation: slideInRight 0.8s ease-out forwards;
    }

    .animate-scale-in {
        animation: scaleIn 0.6s ease-out forwards;
    }

    .delay-100 { animation-delay: 0.1s; }
    .delay-200 { animation-delay: 0.2s; }
    .delay-300 { animation-delay: 0.3s; }
    .delay-400 { animation-delay: 0.4s; }
    .delay-500 { animation-delay: 0.5s; }

    .stat-card {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .stat-card:hover {
        transform: translateX(12px) scale(1.02);
        box-shadow: -8px 0 0 0 #dc2626;
    }

    .cta-button {
        position: relative;
        overflow: hidden;
    }

    .cta-button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
        transition: left 0.5s;
    }

    .cta-button:hover::before {
        left: 100%;
    }

    .gradient-text {
        background: linear-gradient(135deg, #1f2937 0%, #6b7280 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .background-pattern {
        background-image:
                radial-gradient(circle at 20% 50%, rgba(220, 38, 38, 0.03) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(220, 38, 38, 0.03) 0%, transparent 50%);
    }

    .floating {
        animation: floating 3s ease-in-out infinite;
    }

    @keyframes floating {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
    }
</style>