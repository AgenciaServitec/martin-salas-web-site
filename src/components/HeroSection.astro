---
import HeroAnimations from "./animations/HeroAnimations.astro";
---

<section
        id="hero"
        class="relative min-h-screen overflow-hidden flex items-center"
>
    <div id="hero-bg" class="absolute inset-0 will-change-transform">
        <img
                src="/hero.jpg"
                alt="Candidato al Senado del Perú"
                class="w-full h-full object-cover scale-110"
        />
    </div>

    <div class="absolute inset-0 bg-gradient-to-b
    from-black/90
    via-black/70
    to-[#0a0a0a]/95">
    </div>
    <div class="absolute inset-0 opacity-[0.035] pointer-events-none bg-[url('/noise.png')]"></div>

    <div class="relative z-10 w-full">
        <div class="max-w-4xl mx-auto px-6 text-center text-white">

            <p
                    data-hero-item
                    class="inline-flex items-center gap-2 mb-6 px-5 py-2
           text-xs tracking-[0.35em] uppercase
           bg-black/60 backdrop-blur-sm
           border border-white/30
           rounded-full text-white/90"
            >
                Senado de la República · Perú
            </p>
            <h1
                    data-hero-title
                    class="text-4xl md:text-6xl xl:text-7xl font-extrabold leading-[1.1] text-white"
            >
                Un Senado que defienda al Perú
                <span class="block mt-4 text-red-500 drop-shadow-sm">
        con firmeza y honestidad
    </span>
            </h1>


            <div
                    data-hero-item
                    class="mx-auto mt-8 h-px w-24 bg-gradient-to-r from-transparent via-red-500 to-transparent"
            ></div>

            <p
                    data-hero-item
                    class="mt-8 text-base md:text-lg text-white/80 max-w-2xl mx-auto"
            >
                Compromiso con la democracia, la justicia social y
                el desarrollo responsable de nuestro país.
            </p>

            <div
                    data-hero-item
                    class="mt-12 flex flex-col sm:flex-row justify-center gap-4"
            >
                <a
                        href="#propuestas"
                        class="px-10 py-4 rounded-xl
           bg-red-600 text-white font-semibold
           hover:bg-red-700
           ring-2 ring-red-500/40
           shadow-xl shadow-red-600/30
           transition-all"
                >
                    Propuestas legislativas
                </a>


                <a
                        href="#voluntariado"
                        class="px-10 py-4 rounded-xl
           bg-black/70 backdrop-blur-sm
           border border-white/30
           text-white font-semibold
           hover:bg-white hover:text-black
           transition-all"
                >
                    Participa en la campaña
                </a>

            </div>

        </div>
    </div>

    <div
            id="scroll-hint"
            class="absolute bottom-8 left-1/2 -translate-x-1/2
               flex flex-col items-center gap-2 text-white/50"
    >
        <span class="text-[10px] tracking-[0.3em] uppercase">Scroll</span>
        <span class="text-lg">↓</span>
    </div>

</section>

<HeroAnimations />
