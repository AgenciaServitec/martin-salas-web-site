---
const propuestas = [
    {
        titulo: "Seguridad ciudadana",
        texto:
            "Un marco legal firme que respalde a la Policía, fortalezca la prevención y devuelva la tranquilidad a las familias peruanas.",
        icono: `
      <svg xmlns="http://www.w3.org/2000/svg" class="w-full h-full" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
          d="M12 3l7 4v5c0 5-3.5 9-7 10-3.5-1-7-5-7-10V7l7-4z"/>
      </svg>
    `,
        color: "from-red-500 to-orange-600",
        bgGlow: "bg-red-500/20",
        invertido: false,
        stats: [
            { valor: "100%", label: "Apoyo policial" },
            { valor: "24/7", label: "Vigilancia" },
            { valor: "0", label: "Tolerancia" }
        ]
    },
    {
        titulo: "Economía y empleo",
        texto:
            "Reglas claras, inversión responsable y apoyo a las pequeñas y medianas empresas para generar empleo formal en todo el país.",
        icono: `
      <svg xmlns="http://www.w3.org/2000/svg" class="w-full h-full" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
          d="M12 8c-3 0-5 2-5 4s2 4 5 4 5-2 5-4-2-4-5-4zm0-5v2m0 14v2m-9-9h2m14 0h2"/>
      </svg>
    `,
        color: "from-emerald-500 to-teal-600",
        bgGlow: "bg-emerald-500/20",
        invertido: true,
        stats: [
            { valor: "Miles", label: "Empleos nuevos" },
            { valor: "100%", label: "Transparencia" },
            { valor: "∞", label: "Oportunidades" }
        ]
    },
    {
        titulo: "Educación y futuro",
        texto:
            "Educación de calidad conectada al mercado laboral, con tecnología, meritocracia y oportunidades para todas las regiones.",
        icono: `
      <svg xmlns="http://www.w3.org/2000/svg" class="w-full h-full" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
          d="M12 14l9-5-9-5-9 5 9 5zm0 0v6"/>
      </svg>
    `,
        color: "from-blue-500 to-indigo-600",
        bgGlow: "bg-blue-500/20",
        invertido: false,
        stats: [
            { valor: "100%", label: "Cobertura" },
            { valor: "Tech", label: "Modernización" },
            { valor: "∀", label: "Para todos" }
        ]
    }
];
---

<section class="relative bg-neutral-950 text-white overflow-hidden" id="propuestas">

    <div class="relative z-10 py-20">
        <div class="max-w-5xl mx-auto px-6 text-center">
            <div class="inline-flex items-center gap-2 mb-6 px-4 py-2 bg-white/5 border border-white/10 rounded-full backdrop-blur-sm">
                <span class="w-2 h-2 bg-red-500 rounded-full animate-pulse"></span>
                <span class="text-white/70 font-semibold text-sm uppercase tracking-wider">Propuestas concretas</span>
            </div>
            <h2 class="text-4xl md:text-6xl font-extrabold mb-4">
                <span class="bg-gradient-to-r from-white via-red-200 to-white bg-clip-text text-transparent">
                    Un plan de acción real
                </span>
            </h2>
            <p class="text-xl text-white/60 max-w-2xl mx-auto">
                Soluciones concretas para los desafíos más urgentes del Perú
            </p>
        </div>
    </div>

    {propuestas.map((p, index) => (
            <div class="relative h-[200vh]">

                <div class={`absolute inset-0 ${p.bgGlow} opacity-0 blur-3xl transition-opacity duration-1000`} data-propuesta-bg={index}></div>

                <section class="sticky top-0 min-h-screen flex items-center">
                    <div class="max-w-7xl mx-auto px-6 w-full">
                        <div class={`grid grid-cols-1 lg:grid-cols-2 gap-16 items-center ${p.invertido ? 'lg:grid-flow-dense' : ''}`}>

                            <div class={`opacity-0 translate-y-10 transition-all duration-1000 ${p.invertido ? 'lg:col-start-2' : ''}`} data-propuesta>

                                <div class="relative group">
                                    <div class={`absolute -inset-1 bg-gradient-to-r ${p.color} rounded-3xl blur-xl opacity-20 group-hover:opacity-40 transition duration-500`}></div>

                                    <div class="relative bg-neutral-900/80 backdrop-blur-xl p-10 rounded-3xl border border-white/10 shadow-2xl">
                                        <div class={`inline-flex p-5 rounded-2xl bg-gradient-to-br ${p.color} shadow-lg mb-8 transform group-hover:scale-110 group-hover:rotate-3 transition-all duration-300`}>
                                            <div class="w-12 h-12 text-white" set:html={p.icono} />
                                        </div>
                                        <h3 class="text-4xl lg:text-6xl font-extrabold mb-6 leading-tight">
                                        <span class={`bg-gradient-to-r ${p.color} bg-clip-text text-transparent`}>
                                            {p.titulo}
                                        </span>
                                        </h3>
                                        <p class="text-xl text-white/80 leading-relaxed mb-8">
                                            {p.texto}
                                        </p>
                                        <div class="grid grid-cols-3 gap-6 pt-6 border-t border-white/10">
                                            {p.stats.map(stat => (
                                                    <div class="text-center">
                                                        <div class={`text-3xl font-bold bg-gradient-to-r ${p.color} bg-clip-text text-transparent mb-1`}>
                                                            {stat.valor}
                                                        </div>
                                                        <div class="text-sm text-white/60 font-medium">
                                                            {stat.label}
                                                        </div>
                                                    </div>
                                            ))}
                                        </div>
                                        <div class="mt-8">
                                            <a
                                                    href={`#detalle-${index}`}
                                                    class={`inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-gradient-to-r ${p.color} text-white font-semibold hover:shadow-2xl transform hover:scale-105 transition-all duration-300`}
                                            >
                                                Ver detalles
                                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                                                </svg>
                                            </a>
                                        </div>
                                    </div>
                                </div>

                            </div>

                            <div class={`hidden lg:block opacity-0 translate-y-10 transition-all duration-1000 delay-200 ${p.invertido ? 'lg:col-start-1' : ''}`} data-propuesta>
                                <div class="relative">

                                    <div class={`absolute inset-0 flex items-center justify-center`}>
                                        <div class={`w-96 h-96 rounded-full bg-gradient-to-br ${p.color} opacity-20 blur-3xl animate-pulse`}></div>
                                    </div>

                                    <div class="relative z-10 flex items-center justify-center">
                                        <div class={`w-80 h-80 rounded-full bg-gradient-to-br ${p.color} p-1 shadow-2xl transform hover:scale-110 transition-transform duration-500`}>
                                            <div class="w-full h-full rounded-full bg-neutral-900 flex items-center justify-center p-16">
                                                <div class="w-full h-full text-white opacity-80" set:html={p.icono} />
                                            </div>
                                        </div>
                                    </div>

                                    <div class={`absolute top-10 right-10 w-24 h-24 rounded-2xl bg-gradient-to-br ${p.color} opacity-30 blur-xl animate-float`}></div>
                                    <div class={`absolute bottom-10 left-10 w-32 h-32 rounded-2xl bg-gradient-to-br ${p.color} opacity-20 blur-xl animate-float-delayed`}></div>

                                </div>
                            </div>

                        </div>
                    </div>
                </section>

            </div>
    ))}

    <div class="relative z-10 py-32 border-t border-white/10">
        <div class="max-w-5xl mx-auto px-6 text-center">
            <h3 class="text-3xl md:text-5xl font-extrabold mb-8">
                ¿Listo para ser parte del cambio?
            </h3>
            <p class="text-xl text-white/70 mb-12 max-w-2xl mx-auto">
                Estas son solo algunas de nuestras propuestas. Conoce el plan completo y únete a la transformación del Perú.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a
                        href="#plan-completo"
                        class="group relative inline-flex items-center gap-3 px-8 py-4 rounded-xl bg-gradient-to-r from-red-600 to-red-700 text-white font-semibold overflow-hidden shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300"
                >
                    <span class="relative z-10">Descargar plan completo</span>
                    <svg class="relative z-10 w-5 h-5 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10"></path>
                    </svg>
                    <div class="absolute inset-0 bg-gradient-to-r from-red-700 to-red-800 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left"></div>
                </a>

                <a
                        href="#contacto"
                        class="inline-flex items-center gap-2 px-8 py-4 rounded-xl bg-white/5 backdrop-blur-sm text-white font-semibold border-2 border-white/20 hover:border-red-500 hover:bg-white/10 transition-all duration-300 shadow-lg hover:shadow-xl"
                >
                    Contáctanos
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                    </svg>
                </a>
            </div>
        </div>
    </div>

</section>

<style>
    @keyframes float {
        0%, 100% {
            transform: translateY(0) rotate(0deg);
        }
        50% {
            transform: translateY(-20px) rotate(5deg);
        }
    }

    @keyframes float-delayed {
        0%, 100% {
            transform: translateY(0) rotate(0deg);
        }
        50% {
            transform: translateY(-30px) rotate(-5deg);
        }
    }

    .animate-float {
        animation: float 6s ease-in-out infinite;
    }

    .animate-float-delayed {
        animation: float-delayed 8s ease-in-out infinite;
        animation-delay: 1s;
    }
</style>

<script>
    const observer = new IntersectionObserver(
        entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.remove('opacity-0', 'translate-y-10');

                    // Activar fondo brillante
                    const index = entry.target.getAttribute('data-propuesta');
                    if (index !== null) {
                        const bg = document.querySelector(`[data-propuesta-bg="${index}"]`);
                        if (bg) {
                            bg.classList.add('opacity-100');
                        }
                    }
                } else {
                    // Desactivar fondo cuando sale de vista
                    const index = entry.target.getAttribute('data-propuesta');
                    if (index !== null) {
                        const bg = document.querySelector(`[data-propuesta-bg="${index}"]`);
                        if (bg) {
                            bg.classList.remove('opacity-100');
                        }
                    }
                }
            });
        },
        { threshold: 0.4 }
    );

    document.querySelectorAll('[data-propuesta]').forEach(el => observer.observe(el));
</script>