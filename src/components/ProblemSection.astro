<section class="relative min-h-screen flex items-center md:py-2 py-32 bg-black text-white overflow-hidden" id="problema">

    <div class="absolute inset-0 bg-gradient-to-b from-black via-black/90 to-black"></div>
    <div class="absolute inset-0 opacity-30" id="parallax-bg"></div>

    <div class="relative z-10 max-w-6xl mx-auto px-6 grid grid-cols-1 md:grid-cols-2 gap-14 items-center">

        <div
                class="opacity-0 translate-y-14 transition-all duration-1000"
                data-reveal
        >
      <span class="block text-xs uppercase tracking-[0.3em] text-white/50 mb-4">
        La realidad
      </span>

            <h2 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">
                El Perú enfrenta
                <span class="block text-white/70">desafíos urgentes</span>
            </h2>

            <p class="text-lg md:text-xl text-white/75 leading-relaxed mb-6">
                Inseguridad, corrupción y desigualdad frenan el desarrollo de millones de peruanos.
                No es un problema nuevo, pero sí uno que exige decisiones firmes y responsables.
            </p>

            <p class="text-lg md:text-xl text-white/75 leading-relaxed">
                El Senado debe volver a representar al país con autoridad,
                transparencia y visión de futuro.
            </p>
        </div>

        <div
                class="opacity-0 translate-y-14 transition-all duration-1000 delay-200"
                data-reveal
        >
            <div class="relative rounded-2xl overflow-hidden shadow-2xl">
                <img
                        src="/logo-martin-salas.png"
                        alt="Desafíos del Perú"
                        class="w-full h-[420px] object-cover scale-105"
                />
                <div class="absolute inset-0 bg-black/30"></div>
            </div>
        </div>

    </div>
</section>

<script>
    const revealObserver = new IntersectionObserver(
        entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.remove('opacity-0', 'translate-y-14');
                }
            });
        },
        { threshold: 0.35 }
    );

    document.querySelectorAll('[data-reveal]').forEach(el => revealObserver.observe(el));

    const bg = document.getElementById('parallax-bg');
    window.addEventListener('scroll', () => {
        const offset = window.scrollY * 0.15;
        bg.style.transform = `translateY(${offset}px)`;
    });
</script>