<section
        id="problema"
        class="relative min-h-screen flex items-center py-36 bg-[#050b14]"
>

    <div class="relative z-10 max-w-7xl mx-auto px-6 grid grid-cols-1 lg:grid-cols-2 gap-24 items-center">

        <div
                class="opacity-0 translate-y-12 transition-all duration-1000"
                data-reveal
        >
            <div class="flex items-center gap-4 mb-8">
                <span class="w-14 h-[2px] bg-[#d4a017]"></span>
                <span class="text-xs font-bold tracking-[0.35em] uppercase text-[#d4a017]">
          Diagnóstico nacional
        </span>
            </div>

            <h2 class="text-4xl md:text-6xl font-black leading-tight text-white mb-10 max-w-xl">
                El Perú enfrenta
                <span class="block text-white/60">
          una crisis profunda
        </span>
            </h2>

            <p class="text-lg md:text-xl text-white/80 leading-relaxed mb-6 max-w-xl">
                La inseguridad, la corrupción y la desigualdad no son hechos aislados,
                sino síntomas de un Estado debilitado.
            </p>

            <p class="text-lg md:text-xl text-white/80 leading-relaxed mb-12 max-w-xl">
                El país necesita
                <span class="font-semibold text-[#d4a017]">
          liderazgo firme, decisiones técnicas y visión de futuro.
        </span>
            </p>
            <ul class="space-y-4 text-white/85 text-base md:text-lg">
                <li class="flex items-start gap-3">
                    <span class="mt-2 w-2 h-2 rounded-full bg-[#d4a017]"></span>
                    Inseguridad ciudadana creciente
                </li>
                <li class="flex items-start gap-3">
                    <span class="mt-2 w-2 h-2 rounded-full bg-[#d4a017]"></span>
                    Corrupción estructural en las instituciones
                </li>
                <li class="flex items-start gap-3">
                    <span class="mt-2 w-2 h-2 rounded-full bg-[#d4a017]"></span>
                    Brechas sociales que siguen ampliándose
                </li>
            </ul>
        </div>

        <div
                class="opacity-0 translate-y-12 transition-all duration-1000 delay-200"
                data-reveal
        >
            <div class="relative bg-white rounded-2xl p-10 shadow-2xl max-w-md mx-auto">

                <img
                        src="/logo-martin-salas.png"
                        alt="Crisis del Perú"
                        class="w-full h-64 object-contain mb-10"
                />

                <div class="border-t border-gray-200 pt-6">
                    <p class="text-xl font-extrabold text-[#002850] mb-2">
                        El problema es evidente
                    </p>
                    <p class="text-base text-gray-700 leading-relaxed">
                        La respuesta debe estar a la altura del momento histórico.
                    </p>
                </div>

                <div class="absolute -top-4 left-10 bg-[#002850] text-white text-xs font-bold tracking-widest px-5 py-2 rounded-full">
                    REALIDAD NACIONAL
                </div>
            </div>
        </div>

    </div>
    <script is:inline>
        const revealObserver = new IntersectionObserver(
            entries => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.remove('opacity-0', 'translate-y-12');
                    }
                });
            },
            { threshold: 0.3 }
        );

        document.querySelectorAll('[data-reveal]').forEach(el => {
            revealObserver.observe(el);
        });
    </script>
</section>
